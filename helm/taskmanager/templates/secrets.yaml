apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
# ✅ Values come from Helm --set or external secret manager
# NEVER hardcode real passwords here
stringData:
  username: {{ .Values.postgresql.auth.username | quote }}
  password: {{ .Values.postgresql.auth.password | quote }}